name: Preview GitHub Pages

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: preview-pages-${{ github.event.pull_request.number || github.ref_name }}
  cancel-in-progress: true

jobs:
  build-deploy:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    env:
      PREVIEW_DIR: previews/${{ github.event.pull_request.number || github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Build preview
        run: npm run build
        env:
          VITE_BASE_PATH: /sdge-rate-explorer/${{ env.PREVIEW_DIR }}/
      - name: Deploy preview
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          destination_dir: ${{ env.PREVIEW_DIR }}
          keep_files: true
          commit_message: Deploy preview ${{ github.event.pull_request.number || github.ref_name }}
